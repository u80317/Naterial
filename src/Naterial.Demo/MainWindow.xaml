<Window x:Class="Naterial.Demo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:nd="http://schemas.naterial.com/winfx/xaml"
        Title="Naterial.Demo"
        FontFamily="{StaticResource FontFamily.Default}"
        FontSize="{StaticResource FontSize.Body2}"
        Background="{DynamicResource Brush.Surface}"
        Foreground="{DynamicResource Brush.OnSurface}"
        Width="1280"
        Height="720"
        MinWidth="320"
        MinHeight="320">

    <WrapPanel ItemWidth="160"
               ItemHeight="160"
               HorizontalAlignment="Center"
               VerticalAlignment="Center"
               Margin="8">
        <Border Background="{DynamicResource Brush.Primary}"
                Margin="0 0 16 16">
            <Border.CornerRadius>
                <MultiBinding Converter="{x:Static nd:ShapeCircularConverter.Default}">
                    <Binding Path="ActualWidth"
                             RelativeSource="{RelativeSource Self}" />
                    <Binding Path="ActualHeight"
                             RelativeSource="{RelativeSource Self}" />
                </MultiBinding>
            </Border.CornerRadius>
        </Border>
        <Border Background="{DynamicResource Brush.Primary}"
                CornerRadius="8"
                Margin="0 0 8 8">
            <Border.Clip>
                <MultiBinding Converter="{x:Static nd:ShapeClipConverter.Default}">
                    <Binding Path="ActualWidth"
                             RelativeSource="{RelativeSource Self}" />
                    <Binding Path="ActualHeight"
                             RelativeSource="{RelativeSource Self}" />
                    <Binding Path="CornerRadius"
                             RelativeSource="{RelativeSource Self}" />
                </MultiBinding>
            </Border.Clip>
        </Border>
        <Border Background="{DynamicResource Brush.Primary}"
                Margin="0 0 8 8">
            <Border.CornerRadius>
                <MultiBinding Converter="{x:Static nd:ShapeCircularConverter.Default}">
                    <Binding Path="ActualWidth"
                             RelativeSource="{RelativeSource Self}" />
                    <Binding Path="ActualHeight"
                             RelativeSource="{RelativeSource Self}" />
                </MultiBinding>
            </Border.CornerRadius>
            <Border.Clip>
                <MultiBinding Converter="{x:Static nd:ShapeClipConverter.Default}">
                    <Binding Path="ActualWidth"
                             RelativeSource="{RelativeSource Self}" />
                    <Binding Path="ActualHeight"
                             RelativeSource="{RelativeSource Self}" />
                    <Binding Path="CornerRadius"
                             RelativeSource="{RelativeSource Self}" />
                </MultiBinding>
            </Border.Clip>
        </Border>
    </WrapPanel>

</Window>
