namespace Naterial
{
    /// <summary>
    /// Declares the keys of base palette modes.
    /// </summary>
    public enum BaseTheme
    {
        /// <summary>
        /// Use the Light mode. Default.
        /// </summary>
        Light,
        /// <summary>
        /// Use the Dark mode.
        /// </summary>
        Dark,
    }
}
